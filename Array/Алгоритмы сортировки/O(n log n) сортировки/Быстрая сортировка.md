

Быстрая сортировка (QuickSort) — это алгоритм сортировки, основанный на парадигме «Разделяй и властвуй». Он выбирает элемент в качестве опорного (pivot) и перераспределяет элементы массива вокруг него, помещая опорный элемент на его правильную позицию в отсортированном массиве.

Алгоритм состоит из следующих основных шагов:

1. **Выбор опорного элемента (Pivot):**  
    Выбирается элемент массива в качестве опорного. Способ выбора может быть разным (первый элемент, последний элемент, случайный элемент или медиана).
    
2. **Разбиение массива (Partition):**  
    Массив перестраивается вокруг опорного элемента. После разбиения все элементы меньше опорного оказываются слева от него, а все элементы больше — справа.
    
3. **Рекурсивный вызов:**  
    Тот же процесс рекурсивно применяется к двум подмассивам.
    
4. **Базовый случай:**  
    Рекурсия останавливается, когда в подмассиве остаётся один элемент, так как он уже отсортирован.
    

---

## Выбор опорного элемента

Существует несколько способов выбора опорного элемента:

- **Всегда выбирать первый (или последний) элемент.**  
    В приведённой ниже реализации используется последний элемент. Недостаток этого подхода — худший случай возникает, если массив уже отсортирован.
    
- **Выбор случайного элемента.**  
    Предпочтительный подход, так как отсутствует шаблон, при котором всегда возникает худший случай.
    
- **Выбор медианы.**  
    Идеальный вариант с точки зрения временной сложности, так как медиану можно найти за линейное время, и разбиение всегда делит массив на две равные части. Однако на практике этот метод медленнее из-за больших констант.
    

---

## Алгоритм разбиения (Partition)

Ключевая часть QuickSort — функция `partition()`. Существует три распространённых алгоритма разбиения, каждый из которых работает за `O(n)`:

- **Наивное разбиение:**  
    Создаётся копия массива: сначала помещаются все меньшие элементы, затем большие. После этого временный массив копируется обратно. Требует `O(n)` дополнительной памяти.
    
- **Разбиение Ломуто (Lomuto Partition):**  
    Используется в этой статье. Простой алгоритм, который отслеживает индекс меньших элементов и выполняет обмены.
    
- **Разбиение Хоара (Hoare’s Partition):**  
    Самый быстрый вариант. Проход выполняется с двух сторон массива, меняя местами большие элементы слева и меньшие справа.
    

---

## Принцип работы алгоритма разбиения Ломуто

Логика проста: мы начинаем с левого элемента и отслеживаем индекс `i` меньших (или равных) элементов. При обходе массива, если встречается элемент меньше опорного, он меняется местами с `arr[i]`. В противном случае элемент игнорируется.

---

## Иллюстрация работы алгоритма QuickSort

На предыдущем шаге мы рассмотрели, как процесс разбиения переставляет элементы массива относительно опорного элемента. Далее тот же метод рекурсивно применяется к левому и правому подмассивам. Каждый раз выбирается новый опорный элемент и выполняется разбиение. Процесс продолжается, пока не останется один элемент, который всегда считается отсортированным. Когда каждый элемент окажется на своей правильной позиции, весь массив будет отсортирован.

---

## Реализация на Python

```python
# функция разбиения
def partition(arr, low, high):

    # выбираем опорный элемент
    pivot = arr[high]

    # индекс меньшего элемента,
    # указывает правильную позицию опорного элемента
    i = low - 1

    # проходим по arr[low..high] и перемещаем
    # все элементы меньше опорного влево
    for j in range(low, high):
        if arr[j] < pivot:
            i += 1
            arr[i], arr[j] = arr[j], arr[i]

    # помещаем опорный элемент после меньших элементов
    arr[i + 1], arr[high] = arr[high], arr[i + 1]
    return i + 1


# реализация функции QuickSort
def quickSort(arr, low, high):
    if low < high:

        # pi — индекс опорного элемента
        pi = partition(arr, low, high)

        # рекурсивная сортировка элементов
        # меньше и больше опорного
        quickSort(arr, low, pi - 1)
        quickSort(arr, pi + 1, high)
```


---

## 🔥 Итеративный QuickSort (без рекурсии, без TLE)

```python
import random

def partition(arr, low, high):
    pivot_index = random.randint(low, high)
    arr[pivot_index], arr[high] = arr[high], arr[pivot_index]

    pivot = arr[high]
    i = low - 1

    for j in range(low, high):
        if arr[j] < pivot:
            i += 1
            arr[i], arr[j] = arr[j], arr[i]

    arr[i + 1], arr[high] = arr[high], arr[i + 1]
    return i + 1


def quick_sort_iterative(arr):
    stack = [(0, len(arr) - 1)]

    while stack:
        low, high = stack.pop()
        if low < high:
            pi = partition(arr, low, high)

            # сначала кладём больший отрезок → стек меньше
            if pi - 1 - low > high - (pi + 1):
                stack.append((low, pi - 1))
                stack.append((pi + 1, high))
            else:
                stack.append((pi + 1, high))
                stack.append((low, pi - 1))
```

---

## Анализ сложности Quick Sort

### Временная сложность:

- **Лучший случай:** `Ω(n log n)` — опорный элемент делит массив на две равные части.
    
- **Средний случай:** `Θ(n log n)` — массив делится на две части, но не обязательно равные.
    
- **Худший случай:** `O(n²)` — опорный элемент всегда минимальный или максимальный (например, уже отсортированный массив).
    

### Дополнительная память:

- **Худший случай:** `O(n)` — из-за несбалансированного дерева рекурсии.
    
- **Лучший случай:** `O(log n)` — при сбалансированном разбиении.
    

---

## Преимущества Quick Sort

- Использует подход «разделяй и властвуй».
    
- Эффективен для больших наборов данных.
    
- Низкие накладные расходы по памяти.
    
- Хорошо работает с кэшем, так как сортировка происходит на месте.
    
- Один из самых быстрых универсальных алгоритмов, если стабильность не требуется.
    
- Хвостовая рекурсия позволяет оптимизацию вызовов.
    

---

## Недостатки Quick Sort

- Худшая временная сложность `O(n²)` при неудачном выборе опорного элемента.
    
- Не лучший выбор для маленьких массивов.
    
- **Нестабильный алгоритм:** относительный порядок равных элементов может измениться.
    

---

## Применения Quick Sort

- Сортировка больших массивов в оперативной памяти.
    
- Используется в стандартных библиотечных функциях (например, `std::sort` в C++ и `Arrays.sort` в Java для примитивов).
    
- Упорядочивание записей в базах данных.
    
- Подготовка данных для алгоритмов, требующих отсортированный ввод.
    
- Поиск k-го наименьшего/наибольшего элемента (Quickselect).
    
- Сортировка объектов по нескольким ключам.
    
- Алгоритмы сжатия данных (например, предварительная обработка в кодировании Хаффмана).
    
- Компьютерная графика и вычислительная геометрия.